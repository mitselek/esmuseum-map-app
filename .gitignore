.DS_Store
.env
.nuxt
.output
dist
node_modules
coverage

# SSL certificates for local development
*.pem
*.key
localhost+*

# Translation analysis reports (generated files)
translation-analysis-report.json
*.backup.*

# CVI source/binary assets (keep SVGs and small PDFs, ignore heavy files)
docs/CVI/**/*.ai
docs/CVI/**/*.zip
docs/CVI/**/*.otf
docs/CVI/**/Eesti S*.pdf

# Copilot workspace is now part of the knowledge base
# (previously ignored, now included for documentation)
