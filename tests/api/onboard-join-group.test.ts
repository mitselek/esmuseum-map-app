/**
 * Tests for /api/onboard/join-group server endpoint (FEAT-001)
 *
 * MOVED: Full tests are in tests/unit/server/onboard-endpoints.test.ts
 * which covers all onboard endpoints (check-membership, get-group-info,
 * validate-user, join-group) using MSW + test helpers.
 */
import { describe, it, expect } from 'vitest'

describe('/api/onboard/join-group endpoint', () => {
  it('see tests/unit/server/onboard-endpoints.test.ts for full coverage', () => {
    expect(true).toBe(true)
  })
})
